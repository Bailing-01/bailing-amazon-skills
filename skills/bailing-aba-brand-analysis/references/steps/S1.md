# 步骤 1：导入校验

- **输入**：`file_paths`（1-4个CSV路径，逗号分隔）、`brand`（选填）
- **依赖**：无
- **操作**：agent 读取每个CSV第1行确认品牌名和周次信息，第2行确认列头包含"搜索查询""曝光：曝光总量""品牌曝光占比""点击量""购买"。至少1个文件数据行数 ≥ 10。
- **输出**：确认后的 `file_paths`、`brand` 传给 S2-S4
- **用途**：为 S2-S4 的 run_pipeline.py 提供校验过的输入参数
